[project]
name = "option-pricer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.0.0",
]

[tool.setuptools.packages.find]
where = ["src"]
